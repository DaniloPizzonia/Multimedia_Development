<Window x:Class="PipeApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PipeApplication"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        Title="Pipe Application" Height="350" Width="525">
    <Grid Name="oGrid_Layout">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="176.8"/>
            <ColumnDefinition Width="163*"/>
            <ColumnDefinition Width="178*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Name="oStackPanel_OverviewPage">
            <TextBlock x:Name="oTextBlock_Nachname"
                HorizontalAlignment="Left" 
                TextWrapping="Wrap" 
                Text="Pipes" 
                VerticalAlignment="Top"
                Width="65" />
            <TextBlock x:Name="oTextBox_Nachname"
                HorizontalAlignment="Left" 
                Height="23" 
                Margin="0,0,0,0"
                TextWrapping="Wrap" 
                Text="{Binding PipeCount}" 
                VerticalAlignment="Top" 
                Width="100"/>
        </StackPanel>
    </Grid>
</Window>
